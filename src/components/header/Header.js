import React from 'react';

const Header = () => {
    return (
        <div>
            <h1 className="header-title">Libreto</h1>
            <h2 className="header-menu">placeholder for menu</h2>
        </div>
    );
};

export default Header;
